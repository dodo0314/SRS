# 에이전트 작업 규칙 — SRS (배포본)

Codex는 이 파일을 자동으로 읽고, Claude Code는 [CLAUDE.md](CLAUDE.md)의 `@AGENTS.md` 한 줄로 같은 파일을 읽는다. 2026-09-16 본인 지시로 만들었다.

## 0. 이 저장소는 배포본이다 — 여기서 고치지 않는다

- 앱의 원본은 비공개 저장소 `dodo0314/my-ai-chat`의 `srs/app/`이다. 그쪽 `main`에 앱 변경이 들어오면 GitHub Actions가 `srs/deploy.sh`로 이 저장소를 **통째로 덮어쓴다**(`js/`·`icons/`·`index.html`·`style.css`·`sw.js`·`manifest.webmanifest`). 여기서 고친 앱 파일은 다음 배포 때 사라지거나 배포를 멈추게 한다.
- 앱을 고쳐야 하면 원본 저장소의 `srs/app/`을 고치고, `srs/app/sw.js`의 캐시 버전(`CACHE`)을 올리고, `sh srs/test/run.sh`를 돌린다. 절차는 그쪽 `srs/README.md`에 있다.
- 이 저장소에서 고쳐도 되는 것은 `README.md` · 이 파일 · `CLAUDE.md`뿐이다. `.deployed-from`은 배포 스크립트가 쓰는 출처 표식(원본 저장소의 어느 커밋에서 배포됐는가)이라 평소에는 손으로 바꾸지 않는다. 원본 저장소의 이력이 재작성된 날(2026-09-16)에만 같은 내용의 새 커밋 SHA로 한 번 고쳤다.
- 카드 내용과 학습 기록은 여기 없다. 사용자의 별도 비공개 저장소에 있고, 이 앱은 실행할 때 그것을 읽는다. 카드나 기록을 이 저장소에 넣지 않는다.
- 공개 저장소다. 개인 정보 · 토큰 · 비공개 저장소의 내용을 커밋하지 않는다.

## 1. 공통 규칙 (사본 · 원문은 my-ai-chat/AGENTS.md)

원문이 바뀌면 여기도 고친다. 2026-09-16 사본.

1. **번호·코드는 풀어 쓴다.** 약어(FSRS = 간격 반복 스케줄러, SRS = 간격 반복 시스템 등)가 세션에 처음 나올 때마다 한 줄 설명을 붙인다.
2. **답변은 존댓말로 한다.** 사용자가 반말로 물어도 답변은 존댓말이다. 문서 · 코드 주석 · 커밋 메시지는 `~다` 평서문이다.
3. **모든 답변은 메타점검 블록으로 시작한다.** 형식은 아래이고, 채점기(테스트 · 실행 결과 등 AI 말고 틀림을 알려줄 것)가 없으면 「없음」이라고 쓴다.

   > **메타점검** · 채점기: 있음(무엇) / 없음 · 앵커: 사용자 입장 있음 / 없음 · 유형: 골 있음 → 맡긴다 / 골 없음 → 결론 대신 재료 · 순서: ① … ② … ③ …

4. **초등학생도 알아듣게 쓴다.** 어려운 말은 쉬운 말로 먼저 풀고, 한 문장에 한 가지, 숫자와 목록은 표로, 한 줄로 먼저 답하고 이유는 다음에. 쉽게 쓴다고 반론 · 한계 · 「모른다」를 빼지 않는다.
5. **규칙은 본인 지시가 있으면 그 자리에서 고치고 지금부터 적용된다.**
